# dotfiles

Personal configuration files.

## Contents

- `.vimrc` — Vim configuration with NeoBundle plugins
- `.vim/` — Vim runtime files and plugins
- `.tmux.conf` — tmux configuration (`C-a` prefix, mouse mode, 256-color)
